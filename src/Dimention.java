public class Dimention {
    Point position;
    Point size;
    Dimention(float x, float y, float sizeX, float sizeY){
        position = new Point(x,y);
        size = new Point(sizeX,sizeY);
    }
}
